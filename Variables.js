let name = 'Alice'; // `let` allows you to reassign values
const age = 30; // `const` is for constant values that shouldn't change


function greet(name) {
    console.log('Hello, ' + name + '!');
}
greet('Bob'); // Calls the function with 'Bob' as the argument

